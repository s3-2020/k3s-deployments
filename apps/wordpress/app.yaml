apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-content
  namespace: wordpress
spec:
  storageClassName: truenas-iscsi
  accessModes: ["ReadWriteOnce"]
  resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: wordpress-php-uploads
  namespace: wordpress
data:
  uploads.ini: |
    upload_max_filesize = 64M
    post_max_size = 64M
    memory_limit = 512M
    max_execution_time = 300
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress
  namespace: wordpress
spec:
  replicas: 1
  selector:
    matchLabels: { app: wordpress }
  strategy:
    type: Recreate          # shared RWO volume — never two pods at once
  template:
    metadata:
      labels: { app: wordpress }
    spec:
      containers:
        - name: wordpress
          image: wordpress:7.1.0-php8.3-apache
          ports: [{ containerPort: 80 }]
          env:
            - name: WORDPRESS_DB_HOST
              value: wordpress-db
            - name: WORDPRESS_DB_USER
              value: wordpress
            - name: WORDPRESS_DB_NAME
              value: wordpress
            - name: WORDPRESS_DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: wordpress-db, key: wp-password }
            - name: WORDPRESS_CONFIG_EXTRA
              value: |
                define('WP_MEMORY_LIMIT', '256M');
                define('WP_MAX_MEMORY_LIMIT', '512M');
          volumeMounts:
            - { name: content, mountPath: /var/www/html/wp-content }
            - { name: php-uploads, mountPath: /usr/local/etc/php/conf.d/99-uploads.ini, subPath: uploads.ini }
          resources:
            requests: { cpu: 100m, memory: 512Mi }
      volumes:
        - name: content
          persistentVolumeClaim: { claimName: wordpress-content }
        - name: php-uploads
          configMap: { name: wordpress-php-uploads }
---
apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: wordpress
spec:
  selector: { app: wordpress }
  ports: [{ port: 80, targetPort: 80 }]
