apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: wordpress-db
  namespace: wordpress
spec:
  serviceName: wordpress-db
  replicas: 1
  selector:
    matchLabels: { app: wordpress-db }
  template:
    metadata:
      labels: { app: wordpress-db }
    spec:
      containers:
        - name: mariadb
          image: mariadb:11
          args: ["--binlog-format=ROW"]
          ports: [{ containerPort: 3306 }]
          env:
            - name: MARIADB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef: { name: wordpress-db, key: root-password }
            - name: MARIADB_DATABASE
              value: wordpress
            - name: MARIADB_USER
              value: wordpress
            - name: MARIADB_PASSWORD
              valueFrom:
                secretKeyRef: { name: wordpress-db, key: wp-password }
          volumeMounts:
            - { name: data, mountPath: /var/lib/mysql }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        storageClassName: truenas-iscsi
        accessModes: ["ReadWriteOnce"]
        resources: { requests: { storage: 10Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: wordpress-db
  namespace: wordpress
spec:
  clusterIP: None
  selector: { app: wordpress-db }
  ports: [{ port: 3306 }]
