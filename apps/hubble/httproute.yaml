apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
  namespace: kube-system
spec:
  parentRefs:
    - name: traefik-gateway

  hostnames:
    - hubble.kung.dk

  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
